# Environment Variables

# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id
VITE_GOOGLE_CLIENT_ID=your_google_client_id

# JWT Secret (use a strong random string)
JWT_SECRET=your_random_secret_key

# MongoDB
MONGODB_URI=mongodb://localhost:27017/buildwithai

OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: Your site details for OpenRouter
YOUR_SITE_URL=http://localhost:3000
YOUR_SITE_NAME=BuildWithAi

# Backend Port (optional, defaults to 3001)
PORT=3001

# Frontend API URL (optional, defaults to http://localhost:3001)
# Only change this if your backend is running on a different port/host
VITE_API_URL=http://localhost:3001
